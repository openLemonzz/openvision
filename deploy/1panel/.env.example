# Docker Hub image refs used by deploy/1panel/docker-compose.yml
# Pin these to `latest`, `sha-xxxxxxx`, or a release tag like `v1.2.3`.

VISION_WEB_IMAGE=docker.io/your-dockerhub-username/vision-web:latest
VISION_ADMIN_IMAGE=docker.io/your-dockerhub-username/vision-admin:latest
