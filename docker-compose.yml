services:
  web:
    build:
      context: .
      target: web
    env_file:
      - ./web/.env
    ports:
      - "8080:80"

  admin:
    build:
      context: .
      target: admin
    env_file:
      - ./admin/.env
    ports:
      - "8787:8787"
